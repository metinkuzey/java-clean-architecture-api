spring:
  application:
    name: java-clean-architecture-api

server:
  port: ${APP_PORT:8080}

logging:
  level:
    root: INFO
    com.metinkuzey.cleanapi: INFO

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
