You are Codex. Create a GitHub-ready repository for **Day 01: Clean Architecture REST API Skeleton**.

    ## 0) Goal
    Build: **java-clean-architecture-api**
    Description: Spring Boot REST API skeleton with clean architecture, DTO mapping, validation, global error handling, and OpenAPI.
    Audience: recruiters + senior backend engineers (EU market). Quality should feel production-minded.

    ## 1) Non-negotiables
    - Use **Java 17** (or Java 21 if you prefer) and **Maven**.
    - Follow **Clean Code** and a clear architecture (hexagonal / clean architecture where it makes sense).
    - Include **unit tests** (JUnit 5). Add integration tests where relevant.
    - Include **sane defaults**: validation, error handling, logging.
    - Keep the code runnable locally.
    - Make commits easy: structure files cleanly, no giant monolith classes.
    - No copyrighted content. No secrets committed.

    ## 2) What to implement (tasks)
    Provide:
    1. Project skeleton and package structure
    2. Core implementation for the day’s topic
    3. Tests
    4. Local run instructions
    5. Minimal CI note (GitHub Actions optional)

    **Detailed task list**
    - Create a well-named domain model (where applicable).
    - Add configuration + environment variables where relevant.
    - Add robust edge-case handling.
    - Add a small demo scenario (sample request/response or a simulation runner).
    - Add a `docs/` folder if a diagram or explanation helps.

    ## 3) Suggested repository structure
    - `README.md`
    - `pom.xml`
    - `src/main/java/...`
    - `src/test/java/...`
    - `docs/` (optional)
    - `docker/` or `docker-compose.yml` (optional if relevant)

    ## 4) Acceptance criteria
    - `mvn test` passes.
    - `mvn -q -DskipTests=false test` passes (no flaky tests).
    - If Spring Boot: app starts and exposes at least one useful endpoint.
    - README explains:
      - what it does
      - why design choices were made
      - how to run
      - how to test
      - next improvements

    ## 5) README.md content to create EXACTLY (you can expand “How to run” with concrete commands)
    ```markdown
    # Day 01 — Clean Architecture REST API Skeleton

**Repo name:** `java-clean-architecture-api`  
**Focus:** Spring Boot REST API skeleton with clean architecture, DTO mapping, validation, global error handling, and OpenAPI.

## What you will build
- A small but production-minded project showcasing **senior-level** engineering choices.
- Clean structure, tests, docs, and runnable setup.

## Tech stack (suggested)
- Java 17 (or 21), Maven
- Spring Boot (if applicable)
- JUnit 5 + Mockito
- Docker (if applicable)

## Key requirements
- Clean architecture (clear boundaries, minimal coupling)
- Observability mindset (logs/metrics where relevant)
- Security mindset (input validation, least privilege where relevant)
- Tests + CI-ready structure

## Deliverables
- Source code (generated by Codex)
- `README.md` (this document)
- `prompt.txt` (Codex execution prompt)
- Optional: diagrams under `docs/`

## How to run (placeholder)
> Codex should fill this section with exact commands (mvn/docker-compose/etc.).

## License
MIT

---
Prepared by Metin Kuzey – 2026
    ```

    ## 6) Output
    - Generate the full repository content locally.
    - Ensure formatting is clean.
    - Add short, meaningful comments only where needed.

    Now build the repository.
